# تم إصلاح نسبة الضريبة في الفواتير والـ POS ✅

## التاريخ: 23 فبراير 2026

## المشكلة السابقة
- نسبة الضريبة كانت ثابتة 15% في الكود
- النص "VAT 15%" كان ثابتاً في واجهة POS
- ترتيب الحسابات في قسم الإجماليات لم يكن منطقياً
- الحسابات في الجدول لم تتطابق مع قسم الإجماليات

## الإصلاحات المنفذة

### 1. نسبة الضريبة من قاعدة البيانات
- الآن يتم جلب نسبة الضريبة من جدول `vats` في قاعدة البيانات
- إذا كانت نسبة الضريبة في قاعدة البيانات 16%، ستظهر 16% في الفاتورة والـ POS
- الكود يستخدم: `$sale->vat->rate` للحصول على النسبة
- إذا لم يكن هناك ضريبة محددة، يستخدم 15% كقيمة افتراضية

### 2. إصلاح واجهة POS
- النص "VAT 15%" أصبح ديناميكياً: "VAT 16%" (حسب قاعدة البيانات)
- يتم تحديث النسبة تلقائياً عند اختيار ضريبة مختلفة
- الحسابات في POS تستخدم النسبة من قاعدة البيانات

### 3. إعادة ترتيب قسم الإجماليات في الفواتير
الترتيب الجديد المنطقي:
1. **الإجمالي الفرعي** (Subtotal) - مجموع أسعار المنتجات
2. **الخصم** (Discount) - إن وجد
3. **قيمة التوصيل** (Shipping) - إن وجدت
4. **الإجمالي قبل الضريبة** (Subtotal before VAT) - بعد الخصم والتوصيل
5. **ضريبة القيمة المضافة** (VAT) - بالنسبة من قاعدة البيانات
6. **الإجمالي شامل الضريبة** (Total incl. VAT) - المبلغ النهائي

### 4. توحيد الحسابات
- جدول المنتجات يستخدم نفس نسبة الضريبة من قاعدة البيانات
- قسم الإجماليات يستخدم نفس نسبة الضريبة
- واجهة POS تستخدم نفس نسبة الضريبة
- الحسابات الآن متطابقة في كل مكان

## الملفات المعدلة

### 1. فاتورة A4
**الملف:** `Modules/Business/resources/views/sales/invoices/a4-size.blade.php`

**التغييرات:**
```php
// جلب نسبة الضريبة من قاعدة البيانات
$vatRate = $sale->vat ? ($sale->vat->rate / 100) : 0.15;
$vatPercent = $sale->vat ? $sale->vat->rate : 15;

// استخدام النسبة في حساب الضريبة
$itemTax = $itemSubtotal * $vatRate;
```

### 2. فاتورة الطابعة الحرارية
**الملف:** `Modules/ThermalPrinterAddon/resources/views/sales/3_inch_80mm_simple.blade.php`

**التغييرات:**
- نفس المنطق المطبق في فاتورة A4
- الحسابات متطابقة بين الفاتورتين

### 3. واجهة POS - Sidebar
**الملفات:**
- `Modules/Business/resources/views/sales/partials/sidebar.blade.php`
- `Modules/Business/resources/views/sales/partials/edit-sidebar.blade.php`
- `Modules/Business/resources/views/purchases/partials/create-sidebar.blade.php`
- `Modules/Business/resources/views/purchases/partials/edit-sidebar.blade.php`

**التغييرات:**
```html
<!-- من -->
<span class="summary-label">{{ __('VAT 15%') }}</span>

<!-- إلى -->
<span class="summary-label vat-label">{{ __('VAT') }} <span class="vat-rate-display">16</span>%</span>
```

### 4. JavaScript - حسابات POS
**الملفات:**
- `public/assets/js/custom/sale.js`
- `public/assets/js/custom/purchase.js`

**التغييرات:**
```javascript
// من
let vat_amount = subtotal * 0.15;

// إلى
let vat_rate = parseFloat($(".vat_select option:selected").data("rate")) || 15;
let vat_amount = (subtotal * vat_rate) / 100;

// تحديث النص في الواجهة
$(".vat-rate-display").text(vat_rate);
```

## كيفية الاستخدام

### 1. تحديد نسبة الضريبة في قاعدة البيانات
- اذهب إلى إعدادات الضرائب في النظام
- حدد نسبة الضريبة المطلوبة (مثلاً 16%)
- النظام سيستخدم هذه النسبة تلقائياً في:
  - واجهة POS
  - الفواتير المطبوعة (A4 والحرارية)
  - جميع الحسابات

### 2. إنشاء فاتورة جديدة
- عند إنشاء فاتورة في POS، اختر الضريبة المناسبة
- ستظهر النسبة تلقائياً في الواجهة (مثلاً "VAT 16%")
- الفاتورة ستحسب الضريبة تلقائياً بالنسبة المحددة
- الحسابات ستكون دقيقة ومتطابقة

### 3. تغيير الضريبة أثناء البيع
- يمكنك اختيار ضريبة مختلفة من القائمة المخفية
- النسبة ستتحدث تلقائياً في الواجهة
- الحسابات ستتحدث فوراً

## مثال على الحسابات

### إذا كانت نسبة الضريبة 16%:

**في POS:**
```
منتج 1: 100 ريال × 1 = 100 ريال
الإجمالي الفرعي: 100 ريال
VAT 16%: 16 ريال
الإجمالي: 116 ريال
```

**في الفاتورة:**
```
منتج 1: 100 ريال × 1 = 100 ريال
الضريبة: 100 × 16% = 16 ريال
المجموع: 116 ريال

الإجمالي الفرعي: 100 ريال
الخصم: -10 ريال
قيمة التوصيل: +20 ريال
الإجمالي قبل الضريبة: 110 ريال
ضريبة القيمة المضافة (16%): 17.6 ريال
الإجمالي شامل الضريبة: 127.6 ريال
```

## ملاحظات مهمة

1. **نسبة الضريبة ديناميكية**: تتغير حسب إعدادات قاعدة البيانات
2. **الترتيب منطقي**: الخصم والتوصيل قبل حساب الضريبة
3. **الحسابات دقيقة**: متطابقة بين الجدول والإجماليات
4. **يعمل في كل مكان**: POS، A4، الطابعة الحرارية، المشتريات
5. **التحديث التلقائي**: عند تغيير الضريبة، تتحدث الواجهة فوراً

## الاختبار

### للتأكد من عمل النظام:
1. افتح إعدادات الضرائب
2. تأكد من نسبة الضريبة (مثلاً 16%)
3. افتح POS وأضف منتج
4. تحقق من:
   - النص يظهر "VAT 16%" (وليس 15%)
   - الحسابات صحيحة
   - عند اختيار ضريبة مختلفة، النسبة تتحدث
5. أنشئ فاتورة وتحقق من:
   - نسبة الضريبة في جدول المنتجات
   - نسبة الضريبة في قسم الإجماليات
   - تطابق الحسابات

## الحالة: ✅ جاهز للاستخدام

تم تنفيذ جميع التعديلات المطلوبة وتنظيف الكاش.
النظام الآن يستخدم نسبة الضريبة من قاعدة البيانات بشكل ديناميكي في كل مكان:
- ✅ واجهة POS (Sales & Purchases)
- ✅ الفواتير المطبوعة (A4)
- ✅ الطابعة الحرارية
- ✅ JavaScript calculations
- ✅ Backend calculations
